# DAKB Docker Environment Configuration
# ======================================
#
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# SECURITY WARNING: Never commit .env to source control!

# ==========================================================================
# MongoDB Configuration
# ==========================================================================

# MongoDB root credentials
MONGO_ROOT_USER=dakb
MONGO_ROOT_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD

# MongoDB database name
MONGO_DATABASE=dakb

# MongoDB port (default: 27017)
MONGO_PORT=27017

# ==========================================================================
# Gateway Configuration
# ==========================================================================

# Gateway port (default: 3100)
GATEWAY_PORT=3100

# JWT/HMAC secret for token signing (REQUIRED)
# Generate with: openssl rand -hex 32
DAKB_JWT_SECRET=CHANGE_ME_GENERATE_32_BYTE_HEX_SECRET

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ==========================================================================
# Embedding Service Configuration
# ==========================================================================

# Embedding service port (default: 3101)
EMBEDDING_PORT=3101

# Compute device: cpu, cuda, mps
EMBEDDING_DEVICE=cpu

# Sentence transformer model
EMBEDDING_MODEL=all-mpnet-base-v2

# ==========================================================================
# Optional: Cloud Deployment
# ==========================================================================

# Uncomment for cloud deployment with HTTPS
# DAKB_DEPLOYMENT_MODE=cloud
# DAKB_SSL_CERT=/path/to/cert.pem
# DAKB_SSL_KEY=/path/to/key.pem
